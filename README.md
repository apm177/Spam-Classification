# **Spam Classification**

This project aims to build a machine learning model to classify emails as spam or ham (not spam) using text message data. There are two seperate csv files
that were combined and used together and. After preprocessing email text using NLP techniques, the project trains Logistic Regression, Naive Bayes, and 
Random Forest classifiers, then combines them using a stacking ensemble for improved spam classification.
